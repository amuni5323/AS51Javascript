// Number One: 
function add(a, b){
    return a + b;
}
// Answer
const add = (a, b) => a + b;

// Number Two
function multiply(a, b){
    return a * b;
}
// Answer
const multiply = (a, b) => a * b;

// Number Three
function greet(name){
    return 'hello' + name;
}

// Answer
const greet = (name) => `hello ${name}`; 